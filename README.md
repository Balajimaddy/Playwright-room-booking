# Playwright Room Booking (TypeScript)

Reads `BASE_URL` from `.env` and runs an E2E room booking against https://automationintesting.online.

## Quick start
```bash
npm i
npx playwright install
npm test
```
